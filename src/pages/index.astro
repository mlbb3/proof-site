---
// The spine: the forwardable calling card at `/`.
// Layer 1 of the proof (rigour): the inspectable hero.
// The system layer (Morning Brief, reconcile-or-wait) and the pointer into the
// deep Papote diagnosis spoke follow. Copy is LAST: labels are placeholders.
import Base from '../layouts/Base.astro';
import InspectableHero from '../components/InspectableHero.astro';
---

<Base title="Max Brown, back-office automation" description="Working example. Copy is written last, against the finished build.">
  <main>
    <InspectableHero />
  </main>
</Base>
